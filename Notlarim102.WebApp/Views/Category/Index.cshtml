@using Notlarim102.WebApp
@model IEnumerable<Notlarim102.Entity.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @*@Html.ActionLink(" ", "Create",null, new { @class = "btn-lg bi bi-plus-circle" })*@
    @*<a href="/Category/Create" class="btn btn-primary" title="Yeni Kayit"><i class="bi bi-plus-circle"></i> Yeni Kayit</a>*@
    @Bootstrap.LinkButton("/Category/Create/", "primary", string.Empty, "Yeni Kayit", "bi bi-plus-circle", "Yeni Kayit")

</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreatedOn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ModifiedOn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ModifiedUsername)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreatedOn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ModifiedOn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ModifiedUsername)
        </td>
        <td>

            @*@Html.ActionLink(" ", "Edit", new { id = item.Id }, new { @class = "btn-lg btn-sm bi bi-pencil" })
        @Html.ActionLink(" ", "Details", new { id = item.Id }, new { @class = "btn-lg btn-sm bi bi-ticket-detailed-fill" })
        @Html.ActionLink(" ", "Delete", new { id = item.Id }, new { @class = "btn-lg btn-sm bi bi-trash" })*@
            @Bootstrap.LinkButton("/Category/Edit/" + item.Id, "warning", "btn-sm", "Guncelle", "bi bi-pencil", string.Empty)           
            @*<a href="/Category/Edit/@item.Id" class="btn btn-warning btn-sm" title="Guncelle"> <i class="bi bi-pencil"></i></a>*@
            |
            @Bootstrap.LinkButton("/Category/Details/" + item.Id, "info", "btn-sm", "Detay", "bi bi-search", string.Empty)
            @*<a href="/Category/Details/@item.Id" class="btn btn-info btn-sm" title="Detay"> <i class="bi bi-search"></i></a>*@
            |
            @Bootstrap.LinkButton("/Category/Delete/" + item.Id, "danger", "btn-sm", "Sil", "bi bi-trash", string.Empty)
            @*<a href="/Category/Delete/@item.Id" class="btn btn-danger btn-sm" title="Sil"> <i class="bi bi-trash"></i></a>*@



        </td>
    </tr>
}

</table>
